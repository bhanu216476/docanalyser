server:
  port: 8080

spring:
  application:
    name: spring-boot-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
